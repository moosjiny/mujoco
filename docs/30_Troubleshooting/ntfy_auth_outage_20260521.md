# NTFY 인증 장애 인시던트 / NTFY Auth Outage — 2026-05-21

**Agent:** Moojoco (RTX 4070, host `ml2`, sender-IP `192.168.0.155`)
**상태 / Status:** 거의 복구 (exempt-host 적용 확인, 4개 서비스 기동·active, **최종 검증 대기 — 내일**) / Near-recovered — exempt-host applied (burst all-200), 4 services started & active; final verification deferred to next session
**관련 / Related:** `reference_roops_comm.md` (auto-memory), CLAUDE.md 미결 이슈 #3·#4

> 비밀 미기재 원칙: 토픽명·계정 비밀번호·토큰은 본 문서에 포함하지 않음 (`feedback_roops_topic_secrets`). 자격증명 실물은 Slack `#roops-bridge` / self-DM 에만 존재.

---

## 1. 증상 / Symptoms

| 단계 | 관측 | HTTP |
|---|---|---|
| 초기 | Moojoco 4개 토픽(COMM/HB/SIM/BROADCAST) publish·poll 전부 거부 | **403** forbidden |
| 자격증명 수령 후 테스트 | 모든 요청이 인증 평가 전에 차단 | **429** too many auth failures |
| 서버 health (`/v1/health`) | 정상 | 200 |

- heartbeat 발행기는 30초마다 무성공 publish 반복 (`curl -s -o /dev/null` 로 에러 무시).
- 구독 서비스 3개는 5초 간격 무한 재시작 — 5월 19일 부팅 이후 재시작 카운터 **~24,720회**.
- 로컬 로그 `/tmp/roops_moojoco_*.log` 가 ~3.3 MB 까지 증가, 100% 거부 응답 JSON.

## 2. 근본 원인 / Root cause

1. **403 단계** — 2026-05-18 NTFY 서버 AWS 이전(`hyperbook.com:8880` → 추가로 `https://www.hyperbook.com`) 이후 서버 측 **auth/ACL 이 강제 활성화**됐으나, Moojoco 클라이언트(`~/.roops_moojoco_topics.env`, 4개 systemd 유닛)에는 인증 자격증명이 전혀 없었음.
2. **429 단계** — 자격증명 없는 서비스가 5초 간격으로 장시간 요청 → ntfy `visitor-auth-failure-limit` 한도 초과 → **올바른 자격증명을 줘도 visitor/IP 가 선차단**. (Recon 도 동일 429 재발 보고, 2026-05-21 12:11.)

## 3. 확인된 환경 / Confirmed environment

| 엔드포인트 | health | 무인증 토픽 접근 | 비고 |
|---|---|---|---|
| `https://www.hyperbook.com` | 200 | 거부 | 유효 인증서, Aegis 가 지정한 정규 URL |
| `https://hyperbook.com` | — | — | 인증서 불일치 (cert 는 www 만) |
| `http://hyperbook.com:8880` | 200 | 거부 | 구 엔드포인트, 여전히 동작 |

- 인증 방식: **basic auth** (`moojoco` 계정 + 비밀번호), Aegis 전달. (당초 Moojoco 가 요청한 Bearer 토큰이 아님.)
- 2026-05-19 발급된 `mojo`·`rudex` 토큰은 클라우드 에이전트 전용 — 로컬 Moojoco 가 신원 분리 원칙상 미사용.

## 4. 발신 보고서 / Reports sent (Slack 폴백)

NTFY(L2.5)가 죽어 L1(git)·Slack 으로 폴백. 아래는 Moojoco 가 작성·발신한 메시지 3건.

### 4.1 NTFY L2.5 outage report (2026-05-21 09:07 발신)
- 4개 토픽 403, 서비스 재시작 ~24,720회, 클라이언트 자격증명 부재 보고.
- Asks: ① ACL 확인 ② 토큰 L4 전달 ③ 타 에이전트 cross-check.

### 4.2 토큰 발급 요청 / token request (2026-05-21 12:11 발신)
- Moojoco **전용** 자격증명 요청 (username `moojoco`, `roops-*` 읽기+쓰기).
- mojo/rudex 와 신원 분리 명시, L4/보안 DM 전달 요청, 정규 엔드포인트 확인 요청.

### 4.3 ACK + visitor 차단 해제 요청 / rate-limit reset (2026-05-21 12:50 발신)
- 자격증명 ACK + 429 원인(무인증 서비스 폭주, 정지 완료) 설명.
- Ask: IP `192.168.0.155` auth-failure 카운터 / visitor limit **server-side 리셋** (Recon 건과 묶음).
- 별건: 자격증명이 공개 채널에 평문 게재됨 → 비밀번호 교체 판단 요청.

## 5. 조치 이력 / Actions taken

| 시각(KST) | 조치 |
|---|---|
| 2026-05-21 12:4x | 4개 systemd 서비스 `systemctl --user stop` → 전부 `inactive`. auth-failure 출혈 중단. |
| 2026-05-21 12:50 | Aegis 에 visitor limit 리셋 요청 발신. |
| 2026-05-21 13:4x | Aegis 가 `docker restart ntfy` 로 auth-failure 카운터 전체 초기화 (192.168.0.155 포함). 429(auth-failure) 단계 해제 확인. |
| 2026-05-21 13:45 | `~/.roops_moojoco_topics.env` 에 `NTFY_USER`/`NTFY_PASS` 추가 + `NTFY_BASE` → `https://www.hyperbook.com` 갱신 (chmod 600). |
| 2026-05-21 13:4x | **인증 검증: HB 토픽 인증 읽기 HTTP 200** — basic auth 정상 동작 확인. |
| 2026-05-21 13:4x | 4개 유닛 + `~/.roops_hb_loop.sh` curl 에 `-u` 추가, `curl -s -o /dev/null`→`curl -fsS`(+실패 stderr 로그), 구독 유닛 `RestartSec` 5→30. `daemon-reload` 완료, **서비스는 의도적으로 정지 유지**. |
| 2026-05-21 13:4x | Aegis 에 `192.168.0.155` exempt-host 추가 요청 발신 (Slack #roops-bridge). |

> 주의: 서비스는 `stop` 만 했으므로 **재부팅 시 자동 복귀** → exempt-host 미적용 상태로 켜지면 visitor-request-limit 429 폭주 재발. exempt-host 확인 전에는 `start` 금지.
>
> **신규 블로커:** 인증은 성공하나 Moojoco IP `192.168.0.155` 가 `visitor-request-limit-exempt-hosts` 에 없어 연속 요청이 **429**(auth-failure 아님, request-rate 제한). Recon(`121.140.12.46`) 이 11:58 에 겪고 server.yml exempt 추가로 해결한 것과 동일 패턴. 같은 수정 필요.

## 6. 복구 계획 / Recovery plan (리셋 후)

1. ~~429(auth-failure) 해제 확인~~ ✅ 완료 — Aegis docker restart.
2. ~~`~/.roops_moojoco_topics.env` 에 basic-auth 자격증명 추가 + `NTFY_BASE` → `https://www.hyperbook.com`~~ ✅ 완료 (chmod 600).
3. ~~4개 systemd 유닛 + `~/.roops_hb_loop.sh` 의 curl 에 `-u` 추가~~ ✅ 완료.
4. ~~`curl -s -o /dev/null` → `curl -fsS`~~ ✅ 완료 (+ 실패 시 stderr 로그, 구독 유닛 RestartSec 30).
5. **부분 완료 / 내일 마무리** — Aegis 가 `192.168.0.155` exempt-host 적용 (Slack 응답은 없었으나 burst 3회 전부 200으로 확인). `systemctl --user start` (4개) → **전부 `active`**. ⏳ **남은 검증 (다음 세션):** (a) 재시작 폭주 멈춤 확인 — `NRestarts` ~35k 은 fix 전 누적값, 30초 간격 2회 스냅샷이 같은지(증가 없음) 확인, (b) HB 발행 테스트 200, (c) 구독 로그 `/tmp/roops_moojoco_*.log` 가 403/429 아닌 200/JSON 인지. ⏳ **미완 마무리:** 본 §6·상태 라인 "복구 완료" 확정, DOC_REGISTRY 갱신, Aegis 복구완료 ACK 발신.

> 폴링 감시 루프(cron `a965da9c`)는 사용자 지시로 정지. ⚠️ `moojoco` 비밀번호 평문 노출 → 교체 대기(§7).

## 7. 보류 / Deferred

- **roops-bus clone 지시** (Aegis, 2026-05-21 12:33) — 새 레포 `moosjiny/roops-bus` clone + `pip install -e .`. Phase 4 메시지 버스 본격 도입 건. 별도 결정 대기 (Aegis 권한 검증 원칙 적용).
- **자격증명 평문 노출** — CLAUDE.md 미결 이슈 #4 (`roops-agent` R/W 노출, 2026-05-18) 와 동일 패턴. `moojoco` 비밀번호 교체 여부 Aegis 판단 대기.

---

*Generated by Moojoco (claude-opus-4-7 @ ml2 / RTX 4070).*
