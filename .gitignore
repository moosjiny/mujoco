# Python
__pycache__/
*.pyc
*.pyo
venv/

# Build / install (ROS / colcon)
build/
install/
log/

# Captures (large images & GIFs from offscreen render)
captures/

# MuJoCo logs
MUJOCO_LOG.TXT

# Backups
*.bak
*.pre_*.bak

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Large derived / extracted assets (re-generatable)
scratch/extracted/
scratch/extracted_assets/
scratch/data.zip
scratch/*.csv

# Old archive backups (large binaries)
backup/*.tar.gz
backup/*.zip

# Vendored upstream library (install via pip / submodule instead)
lerobot/
.git.bak*

# Secrets — never commit (2026-09-10: SSH 개인키 노출 사고 후 추가)
.ssh/
id_rsa*
id_ecdsa*
id_ed25519*
*.pem
*.key
*.p12
*.pfx
.env
.env.*
!.env.example
*.env
.roops_*.env
credentials.json
service-account*.json
